/**
 * Barrel export for all API modules.
 * Import individual feature services from here, e.g.:
 *   import { authApi } from '@/api'
 */
export { apiClient }    from './axiosClient'
export { supabase }     from './supabaseClient'
